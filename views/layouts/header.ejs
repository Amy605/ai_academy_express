<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>AI Academy</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
</head>
<body>
  
      <nav>
        <a href="/" class="text-white me-3">Accueil</a>
        <% if (loggedIn) { %>
          <a href="/users/<%= currentUser._id %>" class="text-white me-3">Mon profil</a>
          <a href="/logout" class="text-white">Déconnexion</a>
        <% } else { %>
          <a href="/login" class="text-white me-3">Connexion</a>
          <a href="/signup" class="text-white">Inscription</a>
        <% } %>
      </nav>
    

  <!-- 👇 NE FERME PAS LE <main> ICI, il doit être fermé dans le footer.ejs -->
  <main class="container mt-4">
